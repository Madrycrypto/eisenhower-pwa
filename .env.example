PORT=8080

# Optional, but recommended after first test.
# The app expects Authorization: Bearer <APP_TOKEN> for API calls when set.
APP_TOKEN=change-me-long-random-token

# n8n production webhook URL
N8N_WEBHOOK_URL=https://n8n.maciejmostowski.pl/webhook/eisenhower-intake
